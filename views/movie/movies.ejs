<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/stylesheets/movie-cards.css">

</head>
<body>
    <%- include('../includes/header.ejs') %>
    <main>
        <h1><%= pageTitle %></h1>        
        <div class="movies_grid">
            <% if (movies.length) { %>
                <% movies.forEach(movie => { %>
                    <div class="movie_card">
                        <a href="/movies/movie-details/<%= movie._id%>">
                            <img src="https://i.pinimg.com/564x/e8/11/b2/e811b2175e459dec1af04097a791849d.jpg" alt="<%= movie.title %>">
                        </a>
                        <div>Title: <%= movie.title %> </div>
                        <div><a href="/directors/director-details/<%= movie.directorId%>">Director: <%= movie.directorName %></a></div>
                        <div>Year: <%= movie.year %> </div>
                        <% if (userList) { %>
                            <div>
                            <form action="/movies/delete-<%= userList %>" method="post">
                                <input type="hidden" name="movieId" value="<%= movie._id %>">
                                <button>Delete</button>
                            </form>
                            </div>
                        <% } %>
                    </div>
                <% }) %>
            <% } else { %>
                <h1>No movies to show</h1>
            <% } %>
        </div>
    </main>
</body>
</html>